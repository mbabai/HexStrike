# Runtime
NODE_ENV=development
PORT=3000
APP_BASE_URL=http://localhost:3000

# MongoDB routing
# In code we will use local URI in development and Atlas URI in production.
MONGODB_LOCAL_URI=mongodb://localhost:27017
MONGODB_PROD_URI=mongodb+srv://<username>:<password>@<atlas-cluster>/HexStrike?retryWrites=true&w=majority&appName=byMarcell
# Optional global override (applies regardless of NODE_ENV):
# MONGODB_URI=
# Optional production fallback keys also supported:
# MONGODB_ATLAS_CONNECTION_STRING=
# MONGODB_ATLAS_URI=
# MongoDB-Atlas-ConnectionString=

# MongoDB target
MONGODB_DB_NAME=HexStrike
MONGODB_GAMES_COLLECTION=games

# Require Mongo history persistence (default true in hosted/prod, false locally)
# HEXSTRIKE_REQUIRE_MONGO_HISTORY=
