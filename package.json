{
  "name": "hexstrike",
  "version": "1.0.0",
  "description": "Basic lobby and game skeleton server",
  "main": "dist/index.js",
  "scripts": {
    "build:server": "tsc -p tsconfig.json",
    "build:browser-shared": "tsc -p tsconfig.browser.json && node scripts/fix-browser-esm-imports.cjs public/generated",
    "build": "npm run build:server && npm run build:browser-shared",
    "start": "node dist/index.js",
    "test": "node --test",
    "test:history-store": "npm run build && node scripts/check-history-store.cjs",
    "dev": "npm run build && npm run test && node scripts/run-dev-server.cjs",
    "dev:watch": "npm run build && npm run test && node --watch scripts/run-dev-server.cjs",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "codex": "^0.2.3",
    "mongodb": "^6.15.0"
  }
}
